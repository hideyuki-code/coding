/**
 * 問題3: フィボナッチ数列
 * 
 * n番目のフィボナッチ数を求める
 * @param n 何番目の数を求めるか（0から始まる）
 * @returns n番目のフィボナッチ数
 */
function fibonacci(n: number): number {
  // ここに実装を書く
  
  return 0;
}

// テスト用のコード
const n = 6;
console.log(`フィボナッチ数列の${n}番目は: ${fibonacci(n)}`);

export default fibonacci; 