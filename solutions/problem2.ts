/**
 * 問題2: 文字列の反転と母音カウント
 * 
 * 文字列を反転させて、母音の個数をカウントする
 * @param input 入力文字列
 * @returns 「反転後：XX、母音の数：YY」という形式の文字列
 */
function problem2(input: string): string {
  // ここに実装を書く
  
  return "反転後：XX、母音の数：YY";
}

// テスト用のコード
const sampleInput = "Hello World";
console.log(problem2(sampleInput));

export default problem2; 